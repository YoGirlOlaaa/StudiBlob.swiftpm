import SwiftUI
struct creditsView: View {
    var body: some View {
        
        Text("Credits").font(.system(size: 55, weight: .semibold, design: .serif))
            .padding()
        
    }
}

